# 旧功法传承（临时）

玩家页面 `/game/manual-migration`，洞府“洞府内”快捷区固定显示“旧功法传承”入口，不依赖可兑换资产预查询；没有待兑换资产时由页面展示空状态。后台 `/admin/manual-migration`，仅管理员可查看待兑换统计。迁移范围是有效角色持有的 `creation_products.product_type = gongfa` 成品；材料、神通、宗门心法不在其中。

## 固定补偿

| 品质 | 随机本数 | 境界范围 |
| --- | ---: | --- |
| 凡品 | 1 | 炼气 |
| 灵品 | 2 | 炼气、筑基 |
| 玄品 | 3 | 炼气、筑基、金丹 |
| 真品 | 3 | 筑基、金丹、元婴 |
| 地品 | 3 | 金丹、元婴 |
| 天品 | 4 | 金丹、元婴 |
| 仙品 | 4 | 元婴 |
| 神品 | 6 | 元婴 |

每本旧神品功法另赠感悟果 1 颗，服用后增加 50 点道心感悟，与随机及评分自选玉简叠加。使用通用 `consumable.v1` 灵果事实，玄品，全境界可服；感悟仍遵守 100 上限，超过部分不保留，已满时不可服用。奖励仅在兑换未处理来源时发放，不追溯已删除的来源。

每次必得一本。境界权重与现有参悟一致，按对应玄／真／地／天品典籍基础价格倒数归一化，境界内各功法等概率；天品金丹∶元婴为 5∶1。单本评分 `3000 <= score < 3200` 额外自选 1 本，`score >= 3200` 额外自选 2 本；不叠加、不扣随机次数。自选不限角色境界，可重复选择同名玉简；每本旧功法对应的 0／1／2 本自选玉简须在兑换前选满。零分及异常品质保留待人工核对，不默认降档。

## 上线流程

1. 按正常上线流程备份、完成旧版邮件／寄售等清账，升级至新版数据库。当前 SQL 链不删除 `creation_products`，本功能不需要修改历史 SQL、复制旧功法或执行额外初始化。
2. 上线后，玩家从洞府进入传承页。待兑换列表直接读取当前角色在原表持有的 `gongfa`；品质、评分及归属以兑换事务锁定的原记录为准。兑换成功才删除原记录，失败保留。旧玩法已停用，不再产生新的旧功法成品。
3. 玩家先选好该本旧功法的全部额外玉简，再确认兑换。随机抽取、自选及神品感悟果发放和原记录删除一次完成。后台仅统计原表中尚未兑换的功法及异常来源，不保留已兑换数量或历史凭证。

补偿配置集中在 `src/shared/manual-migration/rules.ts`，迁移期间保持不变。不使用 `app_settings`，不新增迁移表、余额、凭证或请求结果存储，也不需要提前为玩家创建记录。

抽取在服务端进行，完整事务提交后才向玩家返回本次随机和自选结果。若请求中断，刷新原功法列表并检查背包／仓库：原记录仍在时可重试；已经成功兑换则原记录已删除，重复请求无法再发奖。工具不保存或重放上次抽取结果。

## 事务与删除

玩家身份来自服务端会话中的当前角色。复用角色 Redis 锁、数据库角色锁、战斗占用检查和资源提交接口；事务内锁定对应玩家的旧功法记录，按原记录品质与评分核算补偿，校验自选必须选满后才抽取。背包／仓库发放、原记录删除及普通资源变更同事务提交，失败全部回滚。原记录不存在时拒绝发奖，无单独的领取接口或迁移账本。

原表中有效角色的旧功法兑换完成且异常处理完毕后，可删除：

- `src/server/routes/api/manual-migration/`
- `src/react-app/routes/game/manual-migration/`、`src/react-app/routes/admin/manual-migration/`
- `src/shared/manual-migration/`、`src/shared/contracts/manualMigration.ts`、本文档
- API、admin、React router、后台 nav、gameNavigation 的入口，以及 CaveQuickGrid 的快捷入口配置

未兑换完不得直接删除工具，需要另行确定收尾方案。补偿玉简为普通既有物品，感悟果的完整效果存于通用消耗品实例；领取后均不依赖本工具，没有新增永久道具定义、货币或 SQL 表，也没有专属状态需要清理。

## 神品追加补偿验证（2026-09-24）

功法与法宝迁移专项测试共 16 项通过，覆盖神品追加奖励、其他品质不追加、评分奖励独立叠加、通用消耗品格式。`bun run lint`、`bun run build`（含前后端类型检查）、`git diff --check` 通过。浏览器自动化因请求头策略加载失败无法初始化，本轮未完成追加奖励的页面／数据库实测，未注入测试数据。下方记录为追加奖励前的历史验收。

## 本地实测（2026-09-24）

使用用户提供的四条真实旧成品数据生成本地副本，保留品质、评分及 productModel，替换 ID 和归属至已有本地测试角色。仅操作 `127.0.0.1:15432/daoyou_local`，通过真实页面及同源 API 兑换。

| 样本 | 评分 | 实际随机玉简 | 实际自选玉简 |
| --- | ---: | ---: | --- |
| 五元化生经 | 3590 | 6 本元婴 | 长春功 ×2 |
| 道尊权柄·取天而代 | 3610 | 6 本元婴 | 长春功 ×1、明王诀 ×1 |
| 流光阴风真解 | 3940 | 6 本元婴 | 明王诀 ×2 |
| 神霄劫生经 | 3530 | 6 本元婴 | 庚金诀 ×2 |

- 背包页面与数据库一致：共 32 本玉简，合并为 8 个堆叠；四条旧记录全部删除，迁移专属 app_settings 记录为 0。测试前已有的 19 条库存记录逐字段保持不变。
- 少选、超选时页面不可提交；直接调用真实接口，少选、伪造玉简、他人来源均返回 409，超量返回 400，原记录及库存不变。
- 两个页面先后提交同一本，第二次被拒绝；两个并发请求返回 200／429，只有一次发奖。随机与自选出现同名玉简时正确叠加。
- 遇到一次连接中断，数据库确认没有发奖或删除，刷新后成功重试；页面已将网络／JSON 解析错误改为核对列表及库存的提示。
- 360px 弹窗可滚动选择，确认按钮可操作。未实测满背包溢出到仓库、其他品质、评分 3000／3200 边界及事务提交后的断线；规则边界由共享纯测试覆盖。
- 验收后通过角色锁、事务及资源变更流程只删除本次新增的 32 本玉简，核对测试角色库存恢复原值。普通资源事件及递增版本保留，不回退历史。
